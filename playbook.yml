---
- hosts: product 
  connection: local
  become: True

  tasks:
  - name: "Update Product"
    command: ./update_product.py -p {{ productname }} -t {{ cfn_template }} -b {{ s3bucket }} -v {{ version }}
